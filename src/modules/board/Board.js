class Board {
  constructor() {
    //Fills the board with a 10x10 grid of boardcells
    this.board = Array(10).fill(null).map(() => Array(10).fill(null).map(() => new BoardCell()))
  }

  //Function to check if it is a valid coordinate

  //Functions to update the boardcells
}